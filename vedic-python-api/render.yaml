services:
  - type: web
    name: luna-vedic-python-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: VEDIC_API_KEY
        generateValue: true
