{
  "name": "podooo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "node --test tests/*.test.js && python -m unittest discover -s tests -p 'test_*.py'"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.9",
    "sweph": "2.10.3-7"
  }
}
